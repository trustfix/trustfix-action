{
  "name": "trustfix-action",
  "version": "1.0.0",
  "description": "GitHub Action for scanning OIDC trust policy vulnerabilities in GitHub Actions workflows",
  "main": "src/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --source-map --license licenses.txt",
    "test": "jest"
  },
  "keywords": [
    "github-actions",
    "security",
    "oidc",
    "iam",
    "aws",
    "scanner",
    "vulnerabilities"
  ],
  "author": "TrustFix",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@actions/artifact": "^2.1.7",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
